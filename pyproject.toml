[project]
name = "finetuningfactory"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "llamafactory>=0.9.5",
    "pyyaml>=6.0.3",
    "uvicorn>=0.30",
]

[project.scripts]
ftf-api = "backend.app.serve:main"
